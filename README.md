# Capacitance Measurement System on STM32G474RE

This project measures an **unknown capacitance** by exciting an **RC circuit** with a PWM signal and measuring the **time constant (\tau = R·C)** using STM32 timers plus an analog comparator + DAC threshold.

> Board / MCU: **STM32G474RE** (STM32G4 series)

## Concept (high level)

1. **Generate a PWM excitation** (square wave) with **TIM1 Channel 1**.
2. Feed that PWM through a known **resistor R** into the **unknown capacitor C** (RC charging curve).
3. Turn the capacitor voltage \(U_c\) into a digital edge by comparing it to a fixed threshold using:
   - **DAC** generates a reference voltage corresponding to ~\(0.6\cdot 3.3V \approx 1.98V\) (shown in the diagram as ~1.6V; adjust to your chosen threshold)
   - **Comparator** compares \(U_c\) (+ input) against DAC reference (− input)
   - Comparator output becomes a **square signal** that toggles when \(U_c\) crosses the threshold
4. Use **TIM2** in **Input Capture** mode to measure the delay between:
   - the PWM edge (start)
   - the comparator edge (threshold reached)

That measured delay is directly related to the RC time constant, so with known \(R\) you can compute \(C\).

## How the measurement works

### Signals
- **PWM (excitation)**: generated by **TIM1 CH1** and applied to the RC network.
- **Comparator output**: goes high when \(U_c\) rises above the DAC threshold.

### TIM2 configuration
TIM2 uses **two input capture channels**:

- **TIM2 CH1**: Input Capture, direct mode (connected to the PWM)
- **TIM2 CH2**: Input Capture, direct mode **from remap** (connected to comparator output)

TIM2 is configured as:
- **Slave mode**: **Reset mode**
- **Trigger source**: **TI2FP2**

Meaning: every time the PWM trigger occurs, TIM2 counter is reset, then TIM2 captures until it detects the rising edge from the comparator output.

### What is measured?
Let \(T_d\) be the time from the PWM edge to the moment \(U_c\) reaches the threshold.

For an RC charging curve:

\[
U_c(t)=V_{DD}(1-e^{-t/(RC)})
\]

If the comparator threshold is \(V_{th}\), then:

\[
V_{th}=V_{DD}(1-e^{-T_d/(RC)})
\]

Solving for \(T_d\):

\[
T_d = -RC\,\ln\left(1-\frac{V_{th}}{V_{DD}}\right)
\]

If you choose \(V_{th}=\frac{V_{DD}}{2}\), it simplifies to:

\[
T_d = RC\,\ln(2)
\]

So:

\[
C = \frac{T_d}{R\,\ln(2)}
\]

## Hardware overview

- **TIM1 CH1**: PWM output to RC input
- **R**: known resistor
- **C**: unknown capacitor (sensor / plates / material under test)
- **DAC**: generates \(V_{th}\)
- **COMP**: compares \(U_c\) to \(V_{th}\)
- **TIM2**: captures timing (PWM reference + comparator edge)

## Diagram

![Capacitance measurement concept](docs/concept.jpg)

> If `docs/concept.jpg` does not exist yet, add the provided image to that path (recommended), or update the path above to match your repository.

## Typical workflow

1. Configure peripherals (TIM1 PWM, DAC threshold, COMP, TIM2 input capture + slave reset).
2. Start PWM.
3. For each PWM cycle:
   - TIM2 resets on the trigger
   - TIM2 captures the rising edge of comparator output
   - Compute \(T_d\) from captured counts and timer clock
4. Compute \(C\) using the selected threshold formula.

## Notes / calibration

- The accuracy depends on:
  - resistor tolerance (use a precision resistor)
  - timer clock accuracy
  - comparator/DAC accuracy and chosen threshold
  - PWM frequency (must allow enough time for charge/discharge)
- If you use a threshold other than \(V_{DD}/2\), use the general equation above.

## License

Add a license if you intend to make the project reusable by others.
